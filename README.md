# Img_2_Text
The script gets images from users via the Telegram API, extracts text using OC and sends back text results securely.

## Installation

```bash
pip install -r requirements.txt
python Img_2_Text.py
```

